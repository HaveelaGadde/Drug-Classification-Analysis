
# Treat all text files as UTF-8 and normalize line endings to LF
* text=auto eol=lf

# CSV and notebooks
*.csv text
*.ipynb text

# Python scripts
*.py text eol=lf

# Markdown
*.md text

# Ignore system files
*.DS_Store binary
Thumbs.db binary

# Git LFS support (if needed for large files)
# Uncomment below if using Git LFS
# *.csv filter=lfs diff=lfs merge=lfs -text
# *.ipynb filter=lfs diff=lfs merge=lfs -text
